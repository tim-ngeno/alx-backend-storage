// List all documents with the name Holberton school
var collection = db.getCollection('School')
var cursor = collection.find({ name: 'Holberton school' })

while (cursor.hasNext()) {
    var document = cursor.next()
    printjson(document)
}
